# Registry Editor

A web application inspired by the Windows Registry, built with Python, Flask, JavaScript, HTML, and CSS.

The application uses a two-panel interface for navigating and managing hierarchical data. It supports creating, editing, renaming, and deleting entries, as well as searching by name or content.
